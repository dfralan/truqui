{
  "name": "truqui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "start": "npm run build && npx serve dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=truqui"
  },
  "dependencies": {
    "@noble/secp256k1": "1.7.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
